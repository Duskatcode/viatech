# Docker local deployment

POSTGRES_DB=biomed
POSTGRES_USER=biomed
POSTGRES_PASSWORD=biomed_password
POSTGRES_PORT=5434

# NUNCA poner aqui una URL externa (Supabase, RDS, etc). El servicio 'api'
# de docker-compose.yml ignora DATABASE_URL a proposito y usa siempre el
# Postgres local ('db') por nombre de red interno. Ver .env.development.example
# para el flujo completo de desarrollo local.

API_PORT=3001
WEB_PORT=8081

# Docker Compose keeps this local compatibility alias. Hosted API services
# should set CORS_ORIGINS directly with a comma-separated allowlist.
FRONTEND_ORIGIN=http://localhost:8081

JWT_ACCESS_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_ACCESS_EXPIRES_IN_SECONDS=900
JWT_REFRESH_EXPIRES_IN_SECONDS=604800

ATTACHMENTS_STORAGE_DIR=/app/storage/attachments

# API docs
# Enable only for local/demo environments. Disable in production.
ENABLE_SWAGGER=true
