services:
  db:
    image: postgres:17-alpine
    container_name: biomed-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-biomed}
      POSTGRES_USER: ${POSTGRES_USER:-biomed}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-biomed_password}
    ports:
      - "${POSTGRES_PORT:-5434}:5432"
    volumes:
      - biomed_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-biomed} -d ${POSTGRES_DB:-biomed}"]
      interval: 10s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: biomed-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: development
      API_PORT: 3000
      # Conexion FIJA al servicio 'db' de este mismo compose, por nombre de
      # red interno de Docker. No lee DATABASE_URL del entorno externo a
      # proposito: este compose es exclusivamente para desarrollo local y
      # NUNCA debe poder apuntar a una base externa (ej. Supabase de
      # produccion), sin importar que .env este cargado en la shell.
      DATABASE_URL: postgresql://${POSTGRES_USER:-biomed}:${POSTGRES_PASSWORD:-biomed_password}@db:5432/${POSTGRES_DB:-biomed}
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-change-me-access-secret}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-change-me-refresh-secret}
      JWT_ACCESS_EXPIRES_IN_SECONDS: ${JWT_ACCESS_EXPIRES_IN_SECONDS:-900}
      JWT_REFRESH_EXPIRES_IN_SECONDS: ${JWT_REFRESH_EXPIRES_IN_SECONDS:-604800}
      ATTACHMENTS_STORAGE_DIR: ${ATTACHMENTS_STORAGE_DIR:-/app/storage/attachments}
      FRONTEND_ORIGIN: ${FRONTEND_ORIGIN:-http://localhost:8081}
      ENABLE_SWAGGER: "true"
    ports:
      - "${HOST_API_PORT:-3001}:3000"
    volumes:
      - biomed_uploads:/app/storage/attachments
      - biomed_reports:/app/apps/api/reports

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        VITE_API_URL: /api/v1
    container_name: biomed-web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "${WEB_PORT:-8081}:80"

volumes:
  biomed_postgres_data:
  biomed_uploads:
  biomed_reports:
